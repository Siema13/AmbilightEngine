<Page
    x:Class="AmbilightEngine.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer>
        <StackPanel Padding="32" Spacing="20" MaxWidth="600">

            <TextBlock Text="Ambilight Engine" FontSize="28" FontWeight="Bold" />
            <TextBlock Text="Panel kontrolny przechwytywania ekranu" Opacity="0.7" />

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="20">
                <StackPanel Spacing="12">
                    <TextBlock x:Name="StatusText" Text="Status: gotowy do startu" FontWeight="SemiBold" />
                    <TextBlock x:Name="FpsText" Text="FPS: --" Opacity="0.7" />

                    <Button x:Name="ToggleButton"
                            Content="Wybierz monitor i uruchom Ambilight"
                            Click="ToggleButton_Click"
                            HorizontalAlignment="Left"
                            Style="{StaticResource AccentButtonStyle}" />
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>