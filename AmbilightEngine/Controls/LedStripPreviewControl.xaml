<UserControl
    x:Class="AmbilightEngine.Controls.LedStripPreviewControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="8" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Ellipse x:Name="ConnectionStatusDot"
                     Width="10" Height="10"
                     Fill="Gray"
                     VerticalAlignment="Center" />

            <TextBlock x:Name="ConnectionStatusText"
                       Grid.Column="1"
                       Text="Podgląd na żywo: łączenie..."
                       Opacity="0.7"
                       VerticalAlignment="Center" />
        </Grid>

        <Viewbox Grid.Row="1" Stretch="Uniform" MaxHeight="260" HorizontalAlignment="Center">
            <Canvas x:Name="PreviewCanvas" Width="700" Height="430" />
        </Viewbox>
    </Grid>
</UserControl>